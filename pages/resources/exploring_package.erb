<script>
function test() {
  data = '{ \
    "code": "' + escape($('#magic-box').val()) + '", \
    "type": "package", \
    "title": "puppet", \
    "munge": { "ensure": "installed" }, \
    "filter": [ "ensure" ] \
  }';
  submitcode('resource', data)
}
</script>

<form id='magic' action='#'>
  <label for='magic-box'>Enter command:</label>
  <quote>Inspect the <code>puppet</code> package with <code>puppet resource</code>.</quote>
  <div class='input-group'>
    <span class='input-group-addon magic-console'>$</span>
    <input type='text' class='form-control magic-console' id='magic-box' placeholder='puppet resource package puppet' aria-label='Command line'>
  </div>
</form>
<br>

<script>
  formFooter();
  catchEnter();
  inputCheck('puppet resource');
</script>
